---
description: Save a durable decision or fact to Cairn's memory
---

Use the `remember` tool from the `cairn` MCP server to store this so future sessions recall it:

$ARGUMENTS

Pick an appropriate kind (decision, fact, gotcha, task, …). Confirm what was stored.
