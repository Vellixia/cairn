---
description: Redact secrets/PII from text before sharing, logging, or committing
---

Use the `sanitize` tool from the `cairn` MCP server on the following text (or, if empty, the text
or output I just referred to):

$ARGUMENTS

Show the redacted result and its sensitivity classification (shareable / needs_review / private).
